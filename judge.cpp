#include <iostream>

void updatePlayer(char c){
	// to sort out player`s chess.

	if (c != '0') {
		// should delete a single piece.
		if (/*currentPlayer is A */) {
			currentPlayer[c-'1'] = 0; 
		} else {
			currentPlayer[c-'A'] = 0;
		}

	} else {
		// nothing to deal with.
	}
}

bool isGameEnd(){
	// is this player end the game.
	flag = 0;


	if (flag == 1) {

	} else {

	}
}